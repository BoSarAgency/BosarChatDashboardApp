/** @type {import('next').NextConfig} */
const nextConfig = {
    // Temporarily disable React StrictMode to fix WebSocket re-rendering issue
    reactStrictMode: false,
    // Configuration options can be added here
};

module.exports = nextConfig;
